let prices: number[] = [10, 20, 30];

let fruit: string[] = ["apple", "banana", "orange"];

//! Careful with assigning a type of empty array
let randomValues: [] = [];

// Ts can infer types, unions
let names = ["João", "Maria", 1];

let arr: (number | string)[] = [1, "2"];
